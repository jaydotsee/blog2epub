# covers/

Put JPG or PNG cover images here and reference them from `blogs.yaml`:

```yaml
books:
  - id: api-management
    cover: covers/api-management.jpg
```

Paths are relative to `blogs.yaml`. A 1200×1600 pixel portrait image (3:4) fits most readers.
Books without a cover get a generated one.
